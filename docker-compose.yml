version: '3.8'

networks:
  nginx-proxy:
    name: iot-net
    external: true

services:
  nadadores-uni:
    container_name: nadadores-uni
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - VIRTUAL_HOST=nadadoresuni.com
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=nadadoresuni.com
      - LETSENCRYPT_EMAIL=jcanahuirev@gmail.com
      - NODE_ENV=production
    networks:
      - nginx-proxy
